/**
 * 样式入口模块。
 *
 * 通过 import 的方式将 `style.scss` 引入构建流程，
 * 从而让打包工具（Rollup + 样式插件）将其编译并产出独立的 CSS 文件。
 *
 * @remarks
 * 该文件不导出任何运行时内容，仅出于副作用而存在。
 *
 * @packageDocumentation
 */

import "./style.scss";
